# URL of the deployed backend API (Render). Leave empty for local dev
# (Vite proxies /api and /uploads to localhost:5000).
# On Vercel, set this to your Render service URL, e.g.:
#   VITE_API_URL=https://daisy-store.onrender.com
VITE_API_URL=
